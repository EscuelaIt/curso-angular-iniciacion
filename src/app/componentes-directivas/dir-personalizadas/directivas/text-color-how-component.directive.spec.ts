import { TextColorHowComponentDirective } from './text-color-how-component.directive';

describe('TextColorHowComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new TextColorHowComponentDirective();
    expect(directive).toBeTruthy();
  });
});
